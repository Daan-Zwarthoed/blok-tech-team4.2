{% extends "layouts/layout.njk" %}
{% block title %}Filter {% endblock %}
{% block content %}
    <div class="matches">
        {% for user in filteredProfiles %}
            <section>
                <article>
                    <p>{{ user.username }}</p>
                    <p>Top game: {{ user.topGame }}</p>
                    <p>Playstyle: {{ user.playstyle }}</p>
                    <p>Playtime: {{ user.playtime }}</p>
                </article>
                <a href="#" class="like">Like</a>
            </section>
        {% endfor %}
    </div>
{% endblock %}